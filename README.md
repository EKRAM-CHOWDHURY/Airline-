# ✈️ Airline++ — Flight Management System

**Airline++** is a console-based C++ application for managing flight bookings.  
It allows users to **book, cancel, and view flights** interactively from a terminal.


## Features
>Book flights with seat selection  
>Cancel existing bookings  
>Save flight and seat data to files (`data.csv`, `seats.map`)  
>Confirmation prompts for important actions  
>Simple and fast text-based user interface  


##  Build & Run

### 🪟 On Windows
```bash
g++ src/main.cpp -o build/airline.exe
.\build\airline.exe

Future Ideas
>Add multiple destinations dynamically
>Add color to the terminal UI
>Add admin/user login
>Show flight details in a table format

